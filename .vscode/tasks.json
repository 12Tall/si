{
    "tasks": [
        {
            "type": "shell",
            "label": "g++ build active file",
            "command": "/usr/bin/g++",
            "args": [
                "-g",
                "${workspaceFolder}/main.cpp",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}.out"
            ],
            "options": {
                // "cwd": "/usr/bin"                
                "cwd": "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}